# My Spec CLI

一个基于 **Specification-Driven Development (SDD)** 理念的轻量级 CLI 工具，帮助使用 Claude Code 进行软件开发。

## 📋 目录

- [快速开始](#快速开始)
- [安装方式](#安装方式)
- [命令参考](#命令参考)
- [初始化后的项目结构](#初始化后的项目结构)
- [开发工作流](#开发工作流)
- [文件夹与模块说明](#文件夹与模块说明)

---

## 🚀 快速开始

### 前置要求

- **Python** 3.11+
- **Git** (版本控制)
- **Claude Code** (AI 编程助手)

### 安装方式

#### 方式一：从 PyPI 安装（推荐）

```bash
pip install my-spec-cli
```

#### 方式二：从源码安装

```bash
git clone https://github.com/ffgzz/myspec.git
cd myspec
pip install -e .
```

### 验证安装

```bash
# 检查版本
myspec version

# 检查依赖工具
myspec check
```

---

## 📖 命令参考

### 1. `myspec init` - 初始化项目

初始化一个新的 SDD 项目，自动下载项目模板并配置开发环境。

#### 基本用法

```bash
# 创建新项目目录并初始化
myspec init my-project

# 在当前目录初始化（目录名作为项目名）
myspec init .
# 或
myspec init --here
```

#### 完整选项

| 选项           | 说明                                            | 示例                           |
| -------------- | ----------------------------------------------- | ------------------------------ |
| `project_name` | 项目名称，使用 `.` 表示当前目录                 | `myspec init demo`             |
| `--script`     | 指定脚本类型：`sh`（Bash）或 `ps`（PowerShell） | `myspec init demo --script ps` |
| `--here`       | 在当前目录初始化                                | `myspec init --here`           |
| `--no-git`     | 跳过 Git 仓库初始化                             | `myspec init demo --no-git`    |

#### 示例

```bash
# Windows 用户推荐
myspec init my-awesome-project --script ps

# macOS/Linux 用户推荐
myspec init my-awesome-project --script sh

# 在已有目录中初始化（会提示确认）
cd existing-folder
myspec init --here

# 不自动创建 Git 仓库
myspec init my-project --no-git
```

#### 初始化流程

执行 `myspec init` 后会依次进行：

1. **环境检查** - 验证 Claude Code 是否可用
2. **下载模板** - 从 GitHub Release 获取最新项目模板
3. **解压文件** - 根据选择的脚本类型配置脚本目录
4. **Git 初始化** - 自动执行 `git init` 并创建初始提交

---

### 2. `myspec check` - 检查环境

检查当前系统是否具备必要的开发工具。

```bash
myspec check
```

输出示例：

```
Checking Tools...

System Check
├── ● Git (available)
└── ● Claude Code (available)
```

会检查以下工具：

- **Git** - 版本控制系统
- **Claude Code** - AI 编程助手

---

### 3. `myspec version` - 查看版本

显示 CLI 工具的版本信息和模板来源。

```bash
myspec version
```

输出示例：

```
╭──────── My Spec CLI ────────╮
│ CLI Version     0.1.0 (Lite) │
│ Template Source ffgzz/myspec │
╰──────────────────────────────╯
```

---

## 📁 初始化后的项目结构

执行 `myspec init my-project` 后，将生成以下目录结构：

```
my-project/
├── .claude/                    # Claude Code 配置目录
│   ├── agents/                 # AI Agent 配置文件
│   │   ├── specification_agent.md  # 需求分析 Agent
│   │   ├── architect_agent.md      # 架构设计 Agent
│   │   ├── blueprint_agent.md      # 蓝图设计 Agent
│   │   ├── implement_agent.md      # 代码实现 Agent
│   │   ├── coder.md                # 编码 Agent
│   │   ├── qa-engineer.md          # 测试工程师 Agent
│   │   ├── database-architect.md   # 数据库架构 Agent
│   │   ├── chart-specialist.md     # 图表专家 Agent
│   │   └── governance_agent.md     # 治理 Agent
│   │
│   ├── commands/               # Claude Code 命令定义
│   │   ├── specify.md          # /specify 命令
│   │   ├── plan.md             # /plan 命令
│   │   ├── blueprint.md        # /blueprint 命令
│   │   ├── tasks.md            # /tasks 命令
│   │   ├── implement.md        # /implement 命令
│   │   ├── constitution.md     # /constitution 命令
│   │   └── submit.md           # /submit 命令
│   │
│   └── skills/                 # AI 技能与领域知识
│       └── project-knowledge/
│           ├── SKILLS.md           # 技能定义文件
│           └── references/
│               └── domain-knowledge.md  # 领域知识文档
│
├── scripts/                    # 自动化脚本（根据选择保留 sh 或 ps1）
│   ├── create-new-feature.*    # 创建新功能分支
│   ├── setup-blueprint.*       # 设置蓝图阶段
│   ├── setup-implement.*       # 设置实现阶段
│   ├── setup-plan.*            # 设置计划阶段
│   ├── setup-tasks.*           # 设置任务阶段
│   └── submit-feature.*        # 提交功能
│
├── templates/                  # 文档模板
│   ├── spec-template.md        # 需求规格模板
│   ├── plan-template.md        # 技术方案模板
│   ├── blueprint-template.md   # 蓝图模板
│   ├── tasks-template.md       # 任务清单模板
│   └── constitution-template.md # 项目宪法模板
│
├── context/                    # 项目上下文（由 /constitution 命令创建）
│   └── constitution.md         # 项目宪法（核心开发原则）
│
├── specs/                      # 生成的规格文档（开发时自动创建）
│   └── {feature-branch}/       # 按功能分支组织
│       ├── spec.md             # 需求规格书
│       ├── plan.md             # 技术方案
│       ├── api.md              # API 契约
│       ├── data-model.md       # 数据模型
│       ├── quickstart.md       # 快速启动指南
│       ├── blueprint.md        # 代码蓝图
│       └── tasks.md            # 实现任务
```

---

## 🔄 开发工作流

My Spec CLI 遵循 **Contract-First SDD（规约驱动开发）** 流程：

```
┌─────────┐    ┌─────────┐    ┌───────────┐    ┌─────────┐    ┌───────────┐
│ Specify │ -> │  Plan   │ -> │ Blueprint │ -> │  Tasks  │ -> │ Implement │
│ (需求)  │    │ (方案)  │    │  (蓝图)   │    │ (任务)  │    │  (实现)   │
└─────────┘    └─────────┘    └───────────┘    └─────────┘    └───────────┘
```

### 阶段说明

#### 1️⃣ Specify（定义需求）

将自然语言描述转化为结构化的需求规格书。

```bash
# 在 Claude Code 中使用
/specify 用户登录功能，支持邮箱和手机号登录
```

产出文件：`specs/{branch}/spec.md`

包含内容：

- 领域词典（统一术语）
- 用户故事与验收场景
- 测试契约定义

#### 2️⃣ Plan（技术方案）

基于需求规格制定技术实现计划。

```bash
/plan
```

产出文件：

- `specs/{branch}/plan.md` - 技术方案
- `specs/{branch}/api.md` - API 契约
- `specs/{branch}/data-model.md` - 数据模型
- `specs/{branch}/quickstart.md` - 快速启动指南

包含内容：

- 技术栈选型
- 架构决策记录（ADR）
- 系统架构图
- 实现策略
- API 接口定义
- 数据实体与数据流

#### 3️⃣ Blueprint（代码蓝图）

生成接口定义、类型声明和测试桩。

```bash
/blueprint
```

产出文件：`specs/{branch}/blueprint.md`

包含内容：

- 文件清单
- 接口签名快照
- 测试覆盖率映射

#### 4️⃣ Tasks（任务分解）

将实现工作分解为可执行的任务清单。

```bash
/tasks
```

产出文件：`specs/{branch}/tasks.md`

包含内容：

- 任务看板
- 详细任务定义
- 依赖关系
- 验证检查点

#### 5️⃣ Implement（代码实现）

按照任务清单逐步实现功能代码。

```bash
/implement T001
```

遵循原则：

- TDD（测试驱动开发）
- 红-绿-重构循环

---

## 📂 文件夹与模块说明

### `.claude/agents/` - AI Agent 配置

存放各阶段使用的 AI Agent 角色定义文件，位于 `.claude` 目录下。每个 Agent 有不同的职责：

| Agent                    | 职责                                   | 使用阶段       |
| ------------------------ | -------------------------------------- | -------------- |
| `specification_agent.md` | 需求分析师，理解用户意图，生成需求规格 | Specify        |
| `architect_agent.md`     | 系统架构师，制定技术方案和架构决策     | Plan           |
| `blueprint_agent.md`     | 蓝图设计师，定义接口和测试桩           | Blueprint      |
| `implement_agent.md`     | 实现工程师，按任务编写代码             | Implement      |
| `coder.md`               | 通用编码助手                           | 全阶段         |
| `qa-engineer.md`         | 测试工程师，编写和维护测试             | 全阶段         |
| `database-architect.md`  | 数据库架构师，设计数据模型             | Plan/Blueprint |
| `governance_agent.md`    | 治理专家，确保规范合规                 | 全阶段         |

### `scripts/` - 自动化脚本

提供工作流自动化脚本，根据初始化时选择的类型包含 `.sh` 或 `.ps1` 文件：

| 脚本                   | 功能                         |
| ---------------------- | ---------------------------- |
| `create-new-feature.*` | 创建新功能分支和相关目录结构 |
| `setup-blueprint.*`    | 准备蓝图阶段的上下文环境     |
| `setup-implement.*`    | 准备实现阶段的上下文环境     |
| `setup-plan.*`         | 准备计划阶段的上下文环境     |
| `setup-tasks.*`        | 准备任务阶段的上下文环境     |
| `submit-feature.*`     | 提交功能并创建 Pull Request  |

### `.claude/skills/` - AI 技能与知识库

存放项目特定的领域知识，位于 `.claude` 目录下，供 AI Agent 在需求分析阶段参考：

```
.claude/skills/
└── project-knowledge/
    ├── SKILLS.md              # 技能激活配置
    └── references/
        └── domain-knowledge.md # 业务领域知识文档
```

**使用场景**：当用户描述需求时，AI 会自动加载领域知识来补充隐含的业务规则和约束。

### `templates/` - 文档模板

包含各阶段输出文档的标准模板：

| 模板                       | 用途                                   |
| -------------------------- | -------------------------------------- |
| `spec-template.md`         | 需求规格书模板，包含用户故事、验收场景 |
| `plan-template.md`         | 技术方案模板，包含架构决策、实现策略   |
| `blueprint-template.md`    | 代码蓝图模板，包含接口定义、测试桩     |
| `tasks-template.md`        | 任务清单模板，包含任务分解、依赖关系   |
| `constitution-template.md` | 项目宪法模板，定义核心开发原则         |

### `.claude/commands/` - Claude Code 命令

存放 Claude Code 的命令定义文件，位于 `.claude` 目录下，用于实现 `/specify`、`/plan` 等交互式命令：

| 命令              | 用途                   |
| ----------------- | ---------------------- |
| `specify.md`      | /specify 命令定义      |
| `plan.md`         | /plan 命令定义         |
| `blueprint.md`    | /blueprint 命令定义    |
| `tasks.md`        | /tasks 命令定义        |
| `implement.md`    | /implement 命令定义    |
| `constitution.md` | /constitution 命令定义 |
| `submit.md`       | /submit 命令定义       |

### `specs/` - 规格文档（运行时生成）

开发过程中自动创建，按功能分支组织所有规格文档：

```
specs/
├── 001-user-auth/
│   ├── spec.md
│   ├── plan.md
│   ├── api.md
│   ├── data-model.md
│   ├── quickstart.md
│   ├── blueprint.md
│   └── tasks.md
└── 002-shopping-cart/
    ├── spec.md
    └── ...
```

### `context/constitution.md` - 项目宪法

由 `/constitution` 命令创建，位于 `context/` 目录下。定义项目的核心开发原则和约束，所有 Agent 和开发活动都必须遵守。通常包含：

- 核心原则（如 TDD 强制执行）
- 代码规范
- 测试要求
- 文档标准

---

## 💡 使用技巧

### 1. 交互式脚本选择

如果不指定 `--script` 参数，CLI 会显示交互式菜单：

```
╭─────── Choose script type (or press Enter) ───────╮
│ ▶ ps  (PowerShell)                                 │
│   sh  (POSIX Shell (bash/zsh))                     │
│                                                    │
│ Use ↑/↓ to navigate, Enter to select, Esc to cancel│
╰────────────────────────────────────────────────────╯
```

### 2. 配合 Claude Code 使用

初始化项目后，在 VS Code 中打开项目，使用 Claude Code 的斜杠命令进行开发：

```bash
# 开始新功能
/specify 实现用户注册功能，支持邮箱验证

# 查看生成的规格后，继续下一阶段
/plan

# 生成代码蓝图
/blueprint

# 分解实现任务
/tasks

# 开始编码
/implement T001
```

### 3. 分支管理

每个功能都应在独立分支上开发，命名格式：`{编号}-{功能名}`

```bash
# 示例分支名
001-user-auth
002-shopping-cart
003-payment-integration
```

---

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
